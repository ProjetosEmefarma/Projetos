Options -Indexes

<IfModule mod_rewrite.c>
    RewriteEngine On
    # Real files (assets) are served directly; everything else goes to index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [QSA,L]
</IfModule>
